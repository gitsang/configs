#!/bin/bash

set -e

echo -e "\n\e[32m===== ipv4 =====\e[0m"
ipv4=$(curl -s ipv4.ip.sb)
echo "$ipv4"
curl -s "https://api.ip.sb/geoip/$ipv4" | jq

echo -e "\n\e[32m===== ipv6 =====\e[0m"
ipv6=$(curl -s ipv6.ip.sb)
echo "$ipv6"
curl -s "https://api.ip.sb/geoip/$ipv6" | jq
